server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identity_service"
#    url: "jdbc:mysql://localhost:3306/identity_service?allowPublicKeyRetrieval=true&useSSL=false"
    username: root
    password: thien123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true